# Python built-in fucntion

### zip
zip function : takes iterable containers and returns a single iterator object, having mapped values from all the containers.
syntax: zip(terators,....) ==> return object of tuples 